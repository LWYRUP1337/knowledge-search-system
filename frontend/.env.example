# Copy to .env and adjust.
# Base URL for all API calls (see docs/API.md for the contract).
VITE_API_BASE_URL=/api
# When true, MSW intercepts requests and serves in-memory mock data.
# Set to false to talk to the real backend.
VITE_ENABLE_MOCKS=true
