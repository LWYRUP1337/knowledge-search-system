name: CI/CD Pipeline

on:
  push:
    branches:
      - '**'
  pull_request:
    branches:
      - main

jobs:

  lint-backend:
    name: Lint Backend ruff
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: 'pip'

      - name: Install ruff
        run: pip install ruff

      - name: Run ruff
        run: ruff check backend/


  lint-frontend:
    name: Lint Frontend tsc
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: frontend/package-lock.json

      - name: Install dependencies
        run: npm ci
        working-directory: frontend

      - name: Run TypeScript check
        run: npm run build
        working-directory: frontend


  test-backend:
    name: Test Backend pytest
    runs-on: ubuntu-latest
    needs: lint-backend

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: 'pip'

      - name: Install dependencies
        run: |
          pip install -r backend/requirements.txt
          pip install pytest

      - name: Run pytest
        run: |
          pytest tests/ -v --tb=short \
            --ignore=tests/test_integration.py \
            --ignore=tests/test_e2e_search.py


  test-integration:
    name: Integration Tests
    runs-on: ubuntu-latest
    needs: [lint-backend, lint-frontend]

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Create .env file
        run: cp .env.example .env

      - name: Start services
        run: |
          docker compose up -d --build postgres elasticsearch redis backend frontend
          sleep 30

      - name: Wait for backend
        run: |
          for i in {1..60}; do
            if curl -sf http://localhost:8000/health > /dev/null 2>&1; then
              echo "Backend is ready (attempt $i)"
              exit 0
            fi
            echo "Attempt $i/60: waiting for backend..."
            sleep 5
          done
          echo "Backend failed to start"
          docker compose logs backend --tail=50
          exit 1

      - name: Upload test document
        run: |
          HTTP_CODE=$(curl -s -o /dev/null -w "%{http_code}" \
            -X POST http://localhost:8000/api/v1/documents/upload \
            -F "file=@tests/fixtures/sample.pdf")
          if [ "$HTTP_CODE" = "200" ] || [ "$HTTP_CODE" = "201" ]; then
            echo "Upload successful (HTTP $HTTP_CODE)"
          elif [ "$HTTP_CODE" = "400" ]; then
            echo "Upload correctly rejected invalid format (HTTP $HTTP_CODE)"
          else
            echo "Unexpected HTTP code: $HTTP_CODE"
            exit 1
          fi

      - name: Search test
        run: |
          RESPONSE=$(curl -s "http://localhost:8000/api/v1/search?q=каникулы")
          if echo "$RESPONSE" | grep -q '"results"'; then
            echo "Search works"
          else
            echo "Search failed"
            exit 1
          fi

      - name: Debug Elasticsearch
        run: |
          echo "Индексы:"
          curl -s http://localhost:9200/_cat/indices
          echo ""
          echo "Документы в индексе:"
          curl -s "http://localhost:9200/documents/_search?pretty&size=1"

      - name: Show logs if failed
        if: failure()
        run: |
          docker compose logs backend --tail=50


  build-and-push:
    name: Build & Push Docker Images
    runs-on: ubuntu-latest
    needs: [lint-backend, lint-frontend, test-backend, test-integration]

    if: github.event_name == 'pull_request' && github.base_ref == 'main'

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build and push backend image
        uses: docker/build-push-action@v5
        with:
          context: ./backend
          push: true
          tags: |
            ${{ secrets.DOCKERHUB_USERNAME }}/knowledge-search-backend:latest
            ${{ secrets.DOCKERHUB_USERNAME }}/knowledge-search-backend:${{ github.sha }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Build and push frontend image
        uses: docker/build-push-action@v5
        with:
          context: ./frontend
          push: true
          tags: |
            ${{ secrets.DOCKERHUB_USERNAME }}/knowledge-search-frontend:latest
            ${{ secrets.DOCKERHUB_USERNAME }}/knowledge-search-frontend:${{ github.sha }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
